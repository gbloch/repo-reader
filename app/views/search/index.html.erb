<div class="layout">
  <div class="content search-wrapper <%= search_wrapper_class %>">
    <%= render "search" %>
  </div>
</div>

<section class="layout">
  <section class="content content--grid">
    <% @repositories.each do |repository| %>
      <%= render partial: "repository_card", locals: { repository: repository } %>
    <% end %>
  </section>
</section>
